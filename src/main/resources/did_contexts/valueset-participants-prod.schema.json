{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://smart.who.int/trust/ValueSet-Participants.schema.json",
  "title": "Participants ValueSet Schema (PROD Environment)",
  "description": "JSON schema for validating participant codes in DID verification methods for PROD environment",
  "type": "object",
  "properties": {
    "@context": {
      "type": "object",
      "properties": {
        "participant": {
          "type": "string",
          "description": "Participant context definition for PROD environment",
          "const": "https://smart.who.int/trust/ValueSet-Participants"
        }
      },
      "additionalProperties": true
    }
  },
  "definitions": {
    "participantValue": {
      "type": "string",
      "pattern": "^[A-Z]{2,3}$",
      "description": "Valid TNG participant codes (2-3 letter country/organization codes) for PROD environment"
    }
  }
}